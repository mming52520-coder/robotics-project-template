# Tests

- `unit/`: deterministic logic and validation.
- `integration/`: interfaces, transports, and component interactions.
- `replay/`: recorded or synthetic time-series scenarios.

Every behavior change must identify the narrowest check that proves it and any safety behavior that must remain invariant.
