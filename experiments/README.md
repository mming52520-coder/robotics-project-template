# Experiments

Create one dated record per experiment. Preserve raw inputs outside generated reports, record code and configuration versions, and never overwrite a previous result.

An experiment is complete when its hypothesis, method, evidence, result, limitations, and decision are recorded.
